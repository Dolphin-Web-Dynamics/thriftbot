<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "ThriftBot" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="ThriftBot">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <% if current_user %>
      <nav class="bg-white shadow-sm border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center space-x-8">
              <%= link_to "ThriftBot", root_path, class: "text-xl font-bold text-indigo-600" %>
              <%= link_to "Dashboard", dashboard_path, class: "text-gray-600 hover:text-gray-900 #{'font-semibold text-gray-900' if controller_name == 'dashboard'}" %>
              <%= link_to "Items", items_path, class: "text-gray-600 hover:text-gray-900 #{'font-semibold text-gray-900' if controller_name == 'items'}" %>
              <%= link_to "Sales", sales_path, class: "text-gray-600 hover:text-gray-900 #{'font-semibold text-gray-900' if controller_name == 'sales'}" %>
              <%= link_to "Imports", csv_imports_path, class: "text-gray-600 hover:text-gray-900 #{'font-semibold text-gray-900' if controller_name == 'csv_imports'}" %>
            </div>
            <div class="flex items-center space-x-4">
              <%= link_to "New Item", new_item_path, class: "bg-indigo-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-indigo-700" %>
              <%= link_to "Change Password", edit_password_path, class: "text-gray-500 hover:text-gray-700 text-sm font-medium" %>
              <%= button_to "Log out", session_path, method: :delete, class: "text-gray-500 hover:text-gray-700 text-sm font-medium" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <% if notice.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg" role="alert">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg" role="alert">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>
  </body>
</html>
